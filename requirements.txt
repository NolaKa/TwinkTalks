# TTS engine
qwen-tts>=0.1.1
torch>=2.10.0
soundfile>=0.13.0

# PDF extraction
pdfplumber>=0.11.0
PyMuPDF>=1.25.0

# EPUB extraction
ebooklib>=0.18
beautifulsoup4>=4.12

# Text processing
nltk>=3.9

# DOCX + RTF + FB2 extractors (FB2 uses stdlib xml.etree)
python-docx>=1.1
striprtf>=0.0.26

# Audio processing
numpy>=1.26,<2.0
pydub>=0.25.1
mutagen>=1.47

# Language auto-detection
langdetect>=1.0

# CLI
tqdm>=4.66

# Web backend (FastAPI). The frontend lives in frontend/ and is built with Vite.
fastapi>=0.110
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9

# Dev
pytest>=8.0
